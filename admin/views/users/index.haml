#inner.row
  = partial 'partials/nav/users'
  %section#body
    =flash_tag :notice
    - if !@users.blank?
      %table
        %thead
          %tr
            %th Name
            %th Email
            %th Created At
            %th Actions
        %tbody
          - @users.each do |user|
            %tr
              %td= "#{user.name} #{user.surname}"
              %td= user.email
              %td
                %a{:href => url(:users, :edit, :id => user.id)} Edit
                &mdash;
                %form.inline{:action => url(:users, :destroy), :method => "post"}
                  %input{:name => "id", :type => "hidden", :value => user.id}/
                  %input{:name => "_method", :type => "hidden", :value => "delete"}/
                  %button.inline{:type => "submit", :value => "Delete"} Delete
                    
    - else
      %p
        No users? How is that possible?
